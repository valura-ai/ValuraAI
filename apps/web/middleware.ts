// Middleware is disabled. No authentication or route protection is enforced at the edge.

export default function () {
  // No-op middleware
}

// No matcher config needed when middleware is disabled.
